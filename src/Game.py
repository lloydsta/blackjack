
class Game():
    def __init__(self, dealer, player):
        self.play_game(dealer, player)

    def play_game(self):
        self.dealer_total = dealer.hand[0].rank + dealer.hand[0].rank
        self.player_total = player.hand[0].rank + player.hand[1].rank
